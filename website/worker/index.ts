import handler from "vinext/server/app-router-entry";

// Gloamere is a static, server-rendered information site. No application
// database, account system, upload service, or image-proxy backend is needed.
export default handler;
