# Knowledge repository conventions

- Put research syntheses in `research/` and decisions in `decisions/`.
- Use frontmatter fields `title`, `status`, `updated`, and `sources`.
- Use relative Markdown links.
- Update an existing record when its question and scope are unchanged.
