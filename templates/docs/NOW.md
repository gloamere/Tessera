# 当前状态

<!-- workflow-os:auto:start -->
## 自动总览

首次运行 `workflow-os sync --render-now` 后，这一段会显示工作包、卡点、待拍板决策与下一步。只有总指挥可以刷新此区域。
<!-- workflow-os:auto:end -->

## 人工备注

在这里保留无法从结构化状态推导的背景、临时优先级或交接说明。`sync --render-now` 不会修改本节。
