# Codex 项目协作规则

本项目使用 `.workflow/README.md` 的工作包、索引和调度规则。`docs/` 下的 Markdown 是唯一事实源；`.workflow/index.sqlite` 只是从 Markdown 生成的本地索引，绝不能作为设计、策划或项目状态的回写来源。

## 总指挥职责

处理一个请求时，总指挥应：

1. 读取 `docs/PROJECT.md`、`docs/NOW.md`，运行 `workflow-os status`。
2. 对已有事项运行 `workflow-os context <work-id>`，只加载本次相关的 brief、工作包和决策。
3. 先从项目文档和代码中消除可自行查明的不确定性。仅当范围、目标、验收、上线风险或实现方向仍有高影响歧义时，才将工作包设为 `waiting_clarification` 并提出一个（最多三个）带推荐默认值的问题。
4. 将跨会话/协作事项建为工作包；将 UI、策划和功能方向建为待拍板决策，并向负责人提供可比较的方案后等待确认。
5. 只为能够独立推进且文件边界明确的子任务派遣 agent；处于 `waiting_clarification` 的工作包不得启动会固化方向的实现任务。
6. 汇总结果、更新 Markdown 事实源，并在负责人拍板和子任务收口后，独占运行 `workflow-os sync --render-now`。
7. 交付前运行 `workflow-os validate`；需要从零恢复索引时运行 `workflow-os rebuild --render-now`。
8. 每轮子 agent 工作后运行 `workflow-os guard <work-id> --outcome progress|no-progress [--error <signature>]`；若返回 `stop`，停止派遣并汇总证据、进入澄清或诊断，不得继续重试。

可用的只读查询包括 `workflow-os status [--json]` 与 `workflow-os context <work-id> [--json]`。用 `workflow-os work create "<名称>"` 和 `workflow-os decision create "<名称>" --work-item <work-id>` 创建对应 Markdown 记录。

## 调研与搜索

研究先检查项目已有 Markdown、代码与 `workflow-os status`，仅在信息缺失时使用 GitHub 或公开网页。用 `workflow-os research create` 建立档案，并在派遣前运行 `workflow-os research plan <research-id>`。quick 可派 1 名 scout；standard 最多 2 名 scout 后由 analyst 综合；deep 必须先将负责人明确确认记录为 approved，再派遣 scout、analyst 与 auditor。

每个派遣任务都要经过 Dispatch Review：任务独立性、允许来源、结构化输出、模型/推理档、轮次预算与是否触及拍板点。模型不可用时，记录实际替代模型和理由。子 agent 只返回结构化摘要与 Evidence Cards；Evidence Card 必须包含 Claim、URL、Source type、Retrieved、Relevance、Caveat。重要结论引用 `[[evidence-<id>]]`，冲突或单一来源须标明不确定性。

## 可选 Superpowers 执行扩展

若项目已由负责人安装 Superpowers，只在明确的软件开发任务中选择性调用 `brainstorming`、`writing-plans`、`subagent-driven-development`、`systematic-debugging` 或 `verification-before-completion`。它不能替代本项目的工作包、拍板、调研证据、文件所有权与 budget guard；不默认启用 worktrees 或强制 TDD。先用 `workflow-os adapter status superpowers` 查看记录；升级候选必须由总指挥研究 Release，使用 `workflow-os adapter check superpowers --version <version> --release-url <url>` 记录，并等待负责人确认后才通过 Codex 插件界面更新。

## 可选 MarkItDown 资料入口

开始涉及 PDF、Office 文档、表格、图片文字或音频的研究前，运行 `workflow-os adapter doctor`。若 `markitdown` 缺失，先向负责人说明来源 `microsoft/markitdown`、所需格式依赖与安装命令，并等待明确授权；不得自动下载、安装或修改 Python 环境。转换结果仅作为带来源信息的 Markdown 资料，不自动升级为项目结论或设计规则。

## 子 agent 边界

子 agent 只接收短简报，并只读/写分配给它的、不与其他 agent 重叠的 Markdown 文件。子 agent 不得：

- 运行 `workflow-os sync` 或 `workflow-os rebuild`；
- 直接打开、编辑或提交 `.workflow/index.sqlite`、WAL/SHM 文件或 `index.lock`；
- 修改 `docs/NOW.md` 的自动总览区、最终决策文件或其他 agent 的工作包。

总指挥必须遵守 `.workflow/agent-budget.yaml`：不超过并发与轮次上限；同一错误达到阈值、连续无进展或 guard 返回 `stop` 时停止。此守卫控制工作轮次与循环，不伪装成平台精确 token 计费数据。

调研、盘点、参考方案和文件边界不重叠的实现可以并行；未确认方向的实现、同一文件编辑和有前置依赖的任务必须顺序执行。
